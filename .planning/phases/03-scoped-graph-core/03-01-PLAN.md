# 03-01: Graph API and Graphology Loading

## Goal

Return scoped graph payloads from SQLite and load them into Graphology.

## Tasks

- [x] Add `load_graph` Tauri command accepting root path, scope path, mode, and limit.
- [x] Query scoped nodes and containment edges from SQLite.
- [x] Load payload into a Graphology graph in React.
