# 01-02: Workspace Open, Scan Job, Metadata Extraction, and Database Schema

## Goal

Let users select a workspace, scan files, and store durable metadata in SQLite.

## Tasks

- [x] Add folder picker and default-root Tauri commands.
- [x] Implement parallel scanner with ignored heavy directories.
- [x] Store file/folder records in SQLite.
- [x] Build containment edges during indexing.

## Verification

- Code compiles through `cargo check`.
- Backend commands are included in Tauri invoke handler.
- Database schema includes files, scan sessions, edges, tags, and file_tags.
