# 04-01: Lazy Loading, Background Scan Behavior, and Cache Reuse

## Goal

Keep the UI responsive and reuse durable metadata across app launches.

## Tasks

- [x] Load explorer children on demand by parent path.
- [x] Load search results only when query runs.
- [x] Load preview only for selected item.
- [x] Persist metadata in SQLite under app data directory.
- [x] Run scan command asynchronously with serialized DB writes.
